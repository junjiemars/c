#include "_signal_.h"

int
main(void)
{
  /* struct sigaction  oact; */

  /* if (sigaction(SIG_BLOCK, NULL, &oact) == -1) */
  /*   { */
  /*     perror(NULL); */
  /*     exit(EXIT_FAILURE); */
  /*   } */

  exit(EXIT_SUCCESS);
}
